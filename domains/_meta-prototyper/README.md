# Legacy Meta-Prototyper Snapshot

Status: legacy / non-runtime alias.

The active domain is:

```text
domains/meta-lab
```

`_meta-prototyper` is an older snapshot of the same system-level idea. It is
kept only for lineage comparison and should not be selected as a Lab domain,
edited as the current generator, or cited as a separate component.

Naming rule:

```text
meta-lab = canonical active name
meta-prototyper = historical/legacy alias
```

If a future change needs generator behavior, modify `domains/meta-lab` and
validate it with `domains/meta-lab/tools/lab_template_validator.py`.
